<div class="secret">
<h1>Saw: secret number game</h1>

<% if @current_user.present? %>
<p>Welcome: <%= @current_user.email %>, I want to play a game</p>
<% end%>

 <form action="/secret/answer"> 
        <label>
            Enter a number between 1 and 10 to guess the random number and survive:
            <input type="search" name="num" placeholder="1, 2,... 8, 9" required autofocus>
        </label>
        <button>Play</button>
</form>
</div>
